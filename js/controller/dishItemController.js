class DishItemController {
    constructor(gsc, view, model) {
        //view.querySelector('.dish-item').forEach(item => item.addEventListener('click', event => gsc.showScreen('DISH_DETAILS')));
    }

}